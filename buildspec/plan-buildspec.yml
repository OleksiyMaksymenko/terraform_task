version: 0.2
phases:
  pre_build:
    commands:
      - terraform init
      - terraform validate
      # - $ curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
      # - unzip awscliv2.zip
      # - sudo ./aws/install
  
  build:
    commands:
      - terraform plan -out=FILE